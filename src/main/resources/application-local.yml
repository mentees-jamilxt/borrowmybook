spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/borrowmybook
    username: postgres
    password: 1234
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect:  org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
